package src;

import java.io.IOException;

public class Main {
    private static final EncryptDecryptApplication encryptDecryptApplication = new EncryptDecryptApplication();

    public static void main(String[] args) throws IOException {
        encryptDecryptApplication.start(args);
    }
}
